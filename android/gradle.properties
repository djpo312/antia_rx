org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Desactiva la compilación incremental de Kotlin: en este entorno (proyecto
# en F:, caché de paquetes en C:) el compilador Kotlin de Gradle falla al
# intentar relativizar rutas entre unidades de disco distintas
# ("this and base files have different roots"). Sin incremental compila
# un poco más lento pero de forma confiable.
kotlin.incremental=false
